/**
 * Maximum number of `alternatives` entries rendered per ambiguous CALLS edge
 * in prompt/xml formatters. When the edge has more than this, formatters show
 * the first N and append `+M more`.
 */
export const MAX_ALTERNATIVES_RENDERED = 3;
